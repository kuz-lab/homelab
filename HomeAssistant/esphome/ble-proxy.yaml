esphome:
  name: ble-proxy-bedroom
  friendly_name: BLE Proxy Bedroom

esp32:
  board: esp32-s3-devkitc-1
  framework:
    type: arduino

logger:
api:
ota:

wifi:
  ssid: !secret wifi_ssid
  password: !secret wifi_password
  ap:
    ssid: "ble-proxy-bedroom"
    password: "fallback123"

captive_portal:

bluetooth_proxy:
  active: true